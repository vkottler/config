---
compiles:
  - name: colors
    configs:
      - "{{config_root}}/color/configs"
    schemas:
      - "{{config_root}}/color/schemas"
    schema_types:
      - "{{config_root}}/color/schema_types"
