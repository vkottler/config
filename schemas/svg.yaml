version: 
  type: string
xmlns:
  type: string
x_offset:
  type: integer
y_offset:
  type: integer
width:
  type: integer
height:
  type: integer
background:
  required: false
  type: dict
  schema:
    theme:
      type: string
      regex: "[a-z_]+"
    variant:
      type: string
      regex: "[a-z_]+"
    hue:
      type: integer
grid:
  required: false
  type: dict
  schema:
    lines:
      type: integer
    color:
      type: string
    width:
      type: integer
border:
  required: false
  type: dict
  schema:
    color:
      type: string
    width:
      type: integer
