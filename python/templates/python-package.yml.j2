---
name: Python Package

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

env:
  SECRETHUB_CREDENTIAL: ${{"{{secrets.SECRETHUB_CREDENTIAL}}"}}

jobs:
  build:

    runs-on: ${{"{{matrix.system}}"}}
    strategy:
      matrix:
        python-version:
{% if "versions" in python %}
{% for version in python["versions"] %}
          - "{{version}}"
{% endfor %}
{% else %}
          - "{{python["default_version"]}}"
{% endif %}
        system:
{% for system in python["systems"] %}
          - {{system}}
{% endfor %}
    env:
      PYTHON_VERSION: ${{"{{matrix.python-version}}"}}
    steps:
      - uses: actions/checkout@master
        with:
          submodules: recursive

      - name: python${{"{{matrix.python-version}}"}} setup
        uses: actions/setup-python@main
        with:
          python-version: ${{"{{matrix.python-version}}"}}
          cache: pip

      - run: pip${{"{{matrix.python-version}}"}} install vmklib

      - run: mk venv
      - run: mk python-sa-types

      - name: lint and build
        run: mk -- -j python-lint yaml python-build-once

      - run: mk python-sa
        env:
          PY_LINT_EXTRA_ARGS: --no-incremental
          PY_LINT_MYPY_EXTRA_ARGS: --no-incremental

      - run: mk python-test
        env:
          PYTEST_EXTRA_ARGS: --cov-report=xml
          PY_TEST_EXTRA_ARGS: --cov-report=xml

      - uses: codecov/codecov-action@master

      - uses: secrethub/actions/env-export@master
        env:
          TWINE_PASSWORD: secrethub://{{python["author_info"]["username"]}}/pypi/api-keys/personal-upload
        if: |
          matrix.python-version == '{{python["default_version"]}}'
          && matrix.system == 'ubuntu-latest'
          && env.SECRETHUB_CREDENTIAL != ''
          && github.ref_name == 'master'

      - run: mk pypi-upload-ci
        env:
          TWINE_USERNAME: __token__
        if: |
          matrix.python-version == '{{python["default_version"]}}'
          && matrix.system == 'ubuntu-latest'
          && env.SECRETHUB_CREDENTIAL != ''
          && github.ref_name == 'master'
